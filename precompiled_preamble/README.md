# Precompiled preamble

Using vanilla TeX/LaTeX and the `%&preamble` directive (and the right commands) it is possible to precompile the preamble (instructions before `\begin{document}` and reduce typesetting time.
